# 更新日志 (Changelog)

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.0.11] - 2026-09-10

### 重大变更
- **安装与更新框架整体迁移到 Velopack 1.2.0**（替代自研 Inno Setup + urllib
  下载 + 静默安装器）。安装版改为 vpk 生成的 one-click `Setup.exe`，绿色版
  由 vpk 直接产出 portable bundle（自带自更新能力），不再区分"形态"。
- **增量更新（delta）**：用户只下载版本间 diff（跨多版本自动回退全量）。
- **构建链新增 .NET SDK 8.0 + vpk 工具**：首次需手动 `dotnet tool install
  vpk`，后续 `scripts\build.bat` 自动化。

### 新增
- **SolidWorks 画图时，中文输入法下也能用单键快捷键**（如中文态按 `E`/`S`/空格
  触发 SW 已绑定的命令）。原理：中文输入法在消息队列层截获字母键用于拼音组合，
  键到不了 SW 窗口过程；本工具用低级键盘钩子在**按键入队之前**判定，命中就把按键
  吞掉并**直接投给 SW 窗口**（`PostMessage`），输入法全程不被触碰，语言栏保持中文。
- 设置页「触发与反馈 → SolidWorks 输入法」新增：**处理方式**（按键直通 / 自动切换
  键盘布局）、**直通的按键**（默认 `A-Z,0-9,SPACE`）、**额外视口类名**（白名单补充）。
- 新增配置项 `ime_assist_mode`（默认 `key`）、`sw_key_list`、`sw_key_extra_classes`
  （旧配置自动迁移补字段）。
- 配置 `update_token`：可选 GitHub token（未认证 60 次/h 限流）。
- `docs/installer-welcome.txt` / `docs/installer-conclusion.txt`（供 vpk
  portable 安装文案；Inno 直装走自身中文向导文案）。
- `docs/sw-ime-shortcut-plan.md`（方案与机制选型）、`scripts/sw_key_probe.py`
  （真机投递探针，排查用，不打包）。

### 安全边界
- 只对 `SLDWORKS.exe` 前台生效：其他软件一律不碰，且每次按键都复核前台窗口。
- 焦点为文本控件或**未识别**控件时一律放行（保守白名单），保证 SW 内打中文不受影响；
  认不出的类名会记进日志便于补白名单。
- 注入事件（`LLKHF_INJECTED`）无条件放行 —— 保证 CAD 的 pyautogui 回退注入键不被吞，
  CAD 手势链路零改动。
- SW 以管理员运行时自动不拦截（UIPI 下投递必失败，宁可不生效也不吞键）；连续投递
  失败会自动停用直通。

### 修复
- **SolidWorks 里右键拖动会弹出 CAD 圆盘**（与 SW 自带「鼠标笔势」打架）：根因是窗口识别的
  兜底启发式 `类名含 afx → 判定为 AutoCAD`，而 SW 主窗类名正是 `Afx:0000…`（MFC），
  于是 SW 被当成 AutoCAD 并套用了它的方案。
  修法：新增设置「不弹圆盘的应用」（`gesture_exclude_apps`，默认 `sldworks`），在**所有
  品牌判定之前**拦下并直接放弃手势 —— 不进状态机、不启动触发线程、不吞事件，右键拖动
  原样交给 SW 自己处理；该名单优先于自定义应用注册。AutoCAD/中望 的判定逻辑未改动。

### 变更（体验修正，2026-09-08）
- **安装器回归纯 Inno 直装向导**（0.0.8 同款体验：中文向导 / 可选目录 /
  覆盖升级 / 原生卸载入口），不再使用"向导壳套 Velopack 引擎"方案。
- **应用内自动更新调整**：Inno 直装版无 Velopack 布局，手动"检查更新"
  改为引导到 GitHub Releases 下载新版安装包；绿色版（vpk portable bundle）
  仍保留 Velopack 增量自更新。
- 移除安装器 splash（无开场动画）。

### 移除
- `src/updater.py` 中自研网络/安装器逻辑（HTML 解析、urllib 下载、子进程
  静默安装器），替换为 velopack 薄封装（仅绿色版布局启用）。
- "向导壳"方案相关脚本产物（内嵌 Velopack 引擎的 Inno 壳）。

### 兼容
- `update_source_url` 旧值（GitHub releases/latest 页 / api.github.com latest
  接口）会被 `normalize_source_url()` 规范化，无需手工迁移。
- Inno 直装版与 0.0.8 共用固定 AppId → 老用户覆盖升级保留配置。

## [0.0.8] - 2026-08-12

### 新增

- 自定义应用支持：可为其他软件（如 SolidWorks）配置手势方案，按可执行文件名 / 窗口标题匹配前台窗口
- 配置界面方案列表改为卡片式：默认折叠、当前方案次标题、卡片拖动排序、展开/折叠动画
- 添加应用对话框：本地浏览选择 exe、全屏窗口捕捉（倒计时 + 悬停窗口红框高亮，倒计时结束自动确认）
- 命令图标库：内置 30 个图标与图标拾取器，扇区可选图标

### 变更

- 手势命令对非 CAD 应用走按键模拟（COM 仅用于 AutoCAD / 中望CAD）
- 更新流程体验改进：去掉二次确认、安装反馈与升级提示
- 默认值调整：浅色界面、graphite 主题、默认关闭启动检查更新

### 修复

- 配置界面方案列表文字对齐、拖动卡顿
- 添加应用按钮点击无响应（对话框控件缺失回归）

## [0.0.7] - 2026-08-12

### 变更

- 打包改为 onedir 目录（绿色版压缩为 zip，安装版递归装入），启动免解压，大幅缩短启动时间
- 启动流程异步化：托盘图标先出现，QSS / 圆盘 / 引擎 / 钩子在事件循环内完成
- 配置界面、更新模块、pyautogui 改为延迟加载，启动只加载运行时必需模块

### 新增

- 设置界面“常规 + 维护”合并为“关于”页，新增版本号与 GitHub 项目主页入口（小猫图标 + 网址链接 + 跳转按钮）

## [0.0.6] - 2026-08-11

### 新增

- 设置界面分类合并（常规 + 维护 → 关于），补充说明图标
- 下拉箭头、复选框对勾改用资源图标

### 修复

- 高 DPI 下圈层判定按逻辑像素换算，与圆盘显示一致
- 命令反馈弹窗松手即弹，命令执行后台化不阻塞主线程

### 性能

- 设置保存防抖，配置重载按需重建界面样式（不再全量刷新）

## [0.0.5] - 2026-08-10

### 新增

- 边界触发优化与圆盘屏幕内显示开关
- 文字大小设置（菜单字号 / 界面字号）
- 自定义四色主题（主色 / 高亮 / 背景 / 悬停，含轨迹与描边）

### 修复

- 边界命令方向以圆盘中心为原点，设置即时生效
- 字号滑杆 divisor 与坏值归一，拖动实时预览并扩大范围
- 更新检查改走 GitHub HTML 页面，规避未认证 API 限流（403）

## [0.0.4] - 2026-08-10

### 新增

- 中英文界面语言切换（i18n）与浅色 / 深色 / 跟随系统界面模式
- 圆盘整体缩放（menu_scale，50%~150%）
- 触发判定改为独立轮询线程：滑动即触发（对齐 Quicker）、多触发键（右键 / 中键 / 侧键）
- 手势轨迹线、命令执行反馈弹窗与手势测试页
- 整包配置备份 / 恢复
- 一键改版本号脚本 `scripts/set_version.py`

### 变更

- 命令执行后台化不阻塞主线程；松手统一取消 CAD 右键菜单，空扇区不再回退内层命令
- 按进程名识别 CAD；圆盘半径几何来源统一（`menu_geometry.py`）
- 事件入队即时唤醒主线程，降低菜单呼出延迟

### 修复

- 跨线程唤醒事件类型错误导致菜单呼出延迟不稳定
- 浅色主题高亮、系统主题切换同步刷新运行时圆盘
- 退出时复位按压状态，增加全局未捕获异常日志

### 文档

- 同步项目指南过时值，新增 CLAUDE.md

## [0.0.3] - 2026-08-10

### 新增

- 安装版 Setup 安装包（Inno Setup）：标准安装向导、开始菜单 / 桌面快捷方式 / 卸载入口、免 UAC、可选择安装位置
- 程序内一键更新：托盘"检查更新" + 设置界面"检查更新"按钮 + 启动时自动检查（24h 间隔），下载新版自动静默覆盖安装
- 设置界面新增"启动时检查更新"开关
- 新增 `src/updater.py` 自动更新模块与 `src/version.py` 运行时版本号

### 变更

- 构建脚本 `scripts/build.bat` 集成 Inno Setup，一次构建产出绿色版 + 安装版双产物
- 发布物新增安装包（Release 附件：绿色版 + 安装版 + 配置模板）

### 修复

- 打包版 https 不可用（spec 误排除 OpenSSL DLL）导致更新功能失效
- 设置界面"启动时检查更新"开关显示状态不同步
- 手动检查更新结果改为弹窗提示（托盘气泡可能被系统屏蔽）

### 文档

- README / README.en 新增安装版与自动更新说明、SmartScreen FAQ
- 新增 `docs/installer-update-plan.md` 实施规划
