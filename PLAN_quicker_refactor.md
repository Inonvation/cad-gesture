# 参考 Quicker 重构计划

## 设计理念对比

| 特性 | Quicker | 当前项目 | 改进目标 |
|------|---------|----------|----------|
| 圆环层数 | 3层（内圈+外圈+扩展圈） | 2层（内圈+外圈） | 增加扩展圈 |
| 配置结构 | 全局+应用覆盖 | 独立Profile | 保持独立，按target分组 |
| 视觉反馈 | 颜色区分状态 | 统一蓝色高亮 | 增加状态色 |
| 取消操作 | ESC/左键取消 | ESC取消 | 增加左键取消 |

## 配置结构（保持独立，无全局配置）

AutoCAD和中望CAD两套配置完全独立，互不干扰：

```json
{
  "settings": { ... },
  "profiles": {
    "AutoCAD-常用": {
      "target": "autocad",
      "sectors": { ... },
      "outer_sectors": { ... },
      "extension_sectors": { ... }  // 新增：扩展圈
    },
    "ZWCAD-常用": {
      "target": "zwcad",
      "sectors": { ... },
      "outer_sectors": { ... },
      "extension_sectors": { ... }  // 新增：扩展圈
    }
  }
}
```

## 重构阶段

### 阶段1：扩展圈

**目标**：鼠标移出外圈后，整个屏幕区域触发扩展圈命令

**改动文件**：
- `src/gesture_engine.py`：计算扇区时增加扩展圈判断
- `src/radial_menu.py`：绘制扩展圈视觉反馈（半透明箭头指示）
- `src/app.py`：处理扩展圈触发事件
- `config/config.json`：增加 `extension_sectors` 配置项

**交互逻辑**：
```
鼠标按下 → 移动超过阈值 → 显示圆盘
  ├─ 在死区内 → 无高亮
  ├─ 在内圈 → 高亮内圈扇区
  ├─ 在外圈 → 高亮外圈扇区
  └─ 在外圈之外 → 高亮扩展圈扇区（整个屏幕区域）
释放 → 触发对应层的命令
```

**视觉设计**：
- 扩展圈不绘制实际圆环，而是在圆盘外显示半透明箭头指示方向
- 中心文字显示扩展圈命令名称
- 扩展圈扇区按8等分覆盖360度方向

**配置迁移**：
- 为现有Profile自动添加空的 `extension_sectors: {}`

### 阶段2：视觉反馈增强

**目标**：颜色编码区分不同状态

**配色方案**：
```python
COLORS = {
    "inner_sector": "#2b5278",          # 内圈正常
    "inner_sector_hl": "#0078D4",       # 内圈高亮
    "outer_sector": "#1e3a54",          # 外圈正常
    "outer_sector_hl": "#0078D4",       # 外圈高亮
    "extension_arrow": "#4a6fa5",       # 扩展圈箭头指示
    "extension_hl": "#0078D4",          # 扩展圈高亮
    "empty": "#1a1a1a",                 # 空扇区（更暗）
    "configured": "#2b5278",            # 已配置（正常亮度）
}
```

**改动文件**：
- `src/radial_menu.py`：增加扩展圈箭头指示器绘制，空扇区更暗
- `src/config_gui.py`：圆盘预览增加颜色编码

### 阶段3：交互逻辑优化

**目标**：更精确的触发判断，更好的取消机制

**改进点**：
1. **精确边界判断**：内外圈边界增加死区，避免误触
2. **取消机制**：左键点击空白区域取消，ESC取消
3. **视觉延迟**：高亮切换增加50ms延迟，避免快速移动时闪烁

**改动文件**：
- `src/gesture_engine.py`：优化扇区计算，增加边界死区
- `src/radial_menu.py`：高亮切换节流
- `src/app.py`：增加左键取消事件处理

### 阶段4：配置GUI增强

**目标**：更好的编辑体验

**改进点**：
1. **扩展圈编辑**：圆盘预览增加扩展圈区域，点击编辑
2. **批量操作**：支持复制扇区配置到其他位置
3. **导入/导出**：支持Profile导出为JSON文件

**改动文件**：
- `src/config_gui.py`：增加扩展圈编辑支持，批量操作

## 实施顺序

1. **阶段1（扩展圈）**：核心交互改进，影响最大
2. **阶段2（视觉反馈）**：用户体验提升
3. **阶段3（交互优化）**：细节打磨
4. **阶段4（GUI增强）**：编辑体验优化

## 技术风险

1. **扩展圈触发距离**：需要用户测试确定最佳阈值
2. **配置迁移**：旧配置需要平滑迁移到新格式（自动添加extension_sectors）
3. **性能**：扩展圈覆盖全屏可能影响绘制性能（需测试）

## 测试计划

每个阶段完成后：
1. `python main.py` 启动测试
2. 在AutoCAD和ZWCAD中分别测试触发
3. 测试边界情况（快速移动、边缘位置等）
4. 测试配置保存/加载
